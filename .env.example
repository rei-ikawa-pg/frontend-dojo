# Cloudflare Web Analytics (beacon site token, public value)
# Set this so <Script> in app/layout.tsx emits the beacon.
# For Workers Builds CI: also register in Cloudflare Dashboard -> Worker -> Settings -> ビルド -> Environment variables.
NEXT_PUBLIC_CF_ANALYTICS_TOKEN=

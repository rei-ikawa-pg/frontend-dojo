軽い (`transform`) と重い (`width`) を同時に有効にしてみます。

フレームは **走る必要のあるフェーズの中で一番深いもの** まで必ず走ります。
つまり、transform と width を同時に動かすと:

- 理論: Layout / Paint / Composite すべて走る
- 実測: `Style+Layout` に時間が乗り、transform の軽さは **相殺** される

実務上これは「重い CSS を 1 つ混ぜた時点でフレームの軽さは崩れる」という教訓になります。
最適化は「どこが重いか」の特定から始まります。

DOM ツリーから切り離されたが、JS からはまだ参照されている要素を **Detached DOM** と呼びます。
参照が残っている限り GC は走らず、「画面には居ないのにヒープにはいる」という直感に反する状況が生まれます。

### 観察のポイント

- 保持サイズを **中 (100K)** 以上にして「1 サイクル実行」を押す
- **Retained** カードのカウントが増える（内訳は保持中の Detached DOM で、「消したつもりで握っている要素」の数）
- **Heap** も Retained の増加と連動して階段状に上がる

### なぜ消えない？

通常、コンポーネント内でしか使わない `useRef` は、コンポーネント自体が
到達不能になれば `ref` ごと GC 対象になり、`ref.current` が指す DOM も
回収されます。問題は、**ref が外に共有されている場合** です。
`forwardRef` で親から渡された / `Context` やグローバル state に格納した /
クロージャで外側の関数が持ち続けている、といったケースでは ref が
コンポーネントより長生きし、`ref.current` の DOM ノードが解放されません。

```tsx
// 外部に共有している ref が長生きする設計のとき、
// cleanup で明示的に参照を切る
const ref = useRef<HTMLDivElement | null>(null)
useEffect(() => {
  return () => {
    ref.current = null // ← 外部に共有されている ref でのみ必要
  }
}, [])
```

ref そのものを共有しない実装（コンポーネント内で閉じている）ならば
この明示的な nullification は不要で、通常は書かなくて構いません。

### WeakRef に切り替えると

`WeakRef` は **GC を妨げない参照** です。
値を取り出すときは `ref.deref()` を呼びますが、結果は `undefined`（既に回収済み）の可能性があるため都度チェックが必要です。
対策を WeakRef に切り替えて同じ操作をすると、GC 発生タイミング次第で
Retained カウントがじわじわ減っていく様子が観察できます（DevTools の GC ボタンを併用すると分かりやすい）。

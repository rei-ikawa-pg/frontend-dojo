最初は最小構成から始めます。左側のカードに描かれるのが **依存木** で、Root が親、A・B・C… が子孫を表します。
ここでは Root → A の 2 ノード（深さ 1、枝分かれ 1）で、右側「state を更新」ボタンを押してみてください。

### 何が起きているか

- Root の `useState` が変化 → Root 自身が再 render
- その JSX に現れる子 A も再 render（memo なし）

Root と A が同時に flash するはずです。カウンタを見ると、クリック回数に比例して total renders が増えます。

### 「伝播」という言葉の正体

React の再 render の「伝播」は、props を介した呼び出しグラフの辿り直しです。
ブラウザ側に特別な仕掛けが動いているわけではなく、
**親の JSX を実行すると子の関数が呼ばれる、という JS レベルのごく自然な挙動** に過ぎません。

ここを掴めると、後のステップで出てくる「memo で止まる / object prop で効かない」も、
同じ JS の枠組みで説明できるようになります。

### 次のステップへ

次は子 A を `React.memo` で包み、props が変わらない限り止まる様子を観察します。

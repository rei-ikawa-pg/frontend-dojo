`width` を毎フレーム変えるとどうなるでしょうか。

width の変更は **「この要素の幅が変わる → 周囲のレイアウトも再計算が必要かもしれない」** という扱いになるため、
Layout から最後まで全部走ります。

右の「Theory vs Actual」テーブルでは:

- **理論** … CSS Triggers が「Layout が必要」と言っている
- **実測** … LoAF API で `Style+Layout` が実際に時間を食っている

の両方に ○ がつくはずです。
FPS も、要素数が増えるほど下がりやすくなることに注目してください。
